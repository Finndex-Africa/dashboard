/**
 * Brand primary blue - single source for dashboard.
 * Keep in sync with frontend: frontend/src/app/globals.css :root { --primary-blue: #0000FF }
 */
module.exports = {
  PRIMARY_BLUE: '#0000FF',
};
